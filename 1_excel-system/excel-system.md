# Phase 1 — Excel System

> Last updated: 2026-07/17
## Overview

The Excel system is the first phase of Project Canneberge. It is a fully functional ETL pipeline and valuation model built inside a macro-enabled Excel workbook (`Project_Canneberge.xlsm`). The system ingests financial data from stockanalysis.com, MarketScreener.com, and fred.stlouisfed.org, transforms it into a clean master lookup table, and feeds a multi-approach business enterprise valuation model.

The system is organized into five sections:

- **1.1 Data Ingestion** — Power Query and VBA fetcher functions that pull raw financial data per ticker
- **1.2 Tier 1 Transforms** — Combiner queries that loop tickers and stack results per statement type
- **1.3 Tier 2 Transform** — Master table (`ALL_FINANCIALS`) combining all statements into one long/tall lookup table
- **1.4 Calculation Layer** — Excel worksheet formulas, valuation approaches, and supporting schedules
- **1.5 Report Output** — Dashboard, projection controls, and valuation summary (Complete/always in progress)

---

## Live Workbook

[Project_Canneberge.xlsm](https://drive.google.com/drive/folders/1Uh4c7jD0-AWuaT15gkVDtA2yjFKLTphn)

---

## Repository Structure
```text
1_excel-system/
├── excel-system.md ← this file
├── _manifest.txt ← auto-generated by Export Code
│
├── 1.1_data-ingestion/ ← section documentation
│ └── 1.1_data-ingestion.md
├── 1.2_transformation-tier1/
│ └── 1.2_transformation-tier1.md
├── 1.3_transformation-tier2/
│ └── 1.3_transformation-tier2.md
├── 1.4_calculation-layer/
│ └── 1.4_calculation-layer.md
├── 1.5_report-output/
│ └── 1.5_report-output.md
│
├── vba/ ← auto-populated by Export Code
│ ├── modCharts.bas
│ ├── modCodeExport.bas
│ ├── modDiagnostics.bas
│ ├── modETL_Global.bas
│ ├── modETL_Logging.bas
│ ├── modETL_Refresh.bas
│ ├── modExtraction.bas
│ ├── modNAVtoggle.bas
│ ├── modPriceFunctions.bas
│ ├── modProjectionToggle.bas
│ ├── modBetaVolCalc.bas
│ ├── modRibbonCallbacks.bas
│ └── (+ 40 sheet code-behind .cls files)
│
├── power-query/ ← auto-populated by Export Code
│ ├── ALL_Beta.m
│ ├── ALL_BS.m
│ ├── ALL_CFS.m
│ ├── ALL_FINANCIALS.m
│ ├── ALL_ForwardEst.m
│ ├── ALL_IS.m
│ ├── ALL_Ratio.m
│ ├── Companies.m
│ ├── FRED.m
│ ├── fnBeta.m
│ ├── fnBS.m
│ ├── fnCFS.m
│ ├── fnCleanFinancialTable.m
│ ├── fnForwardEst.m
│ ├── fnFRED.m
│ ├── fnIS.m
│ ├── fnRatio.m
│ ├── fnSchemaLock.m
│ ├── ALL_TickerPrices.m
│ ├── fnPriceHistory.m
│ ├── Index_prices.m
│ └── Prices_wide.m
│
└── RibbonX/ ← manually maintained
└── customUI14.xml
```
---

## Workbook Sheet Inventory

### Group 1 — Control & Configuration (1 sheet)

| Sheet | Purpose |
|---|---|
| **Control** | Master input sheet. Ticker list (`tblIngest`), subject company config, fiscal year anchors, client info, model parameters, all key named ranges. The "cockpit" of the entire model. |

### Group 2 — ETL Infrastructure (1 sheet)

| Sheet | Purpose |
|---|---|
| **ETL_LOG** | Diagnostic event log. Written by `LogEvent` in `modETL_Logging`. Auto-cleared at start of each pipeline run. |

### Group 3 — Raw Data Staging (2 sheets)

*VBA scrape outputs (MarketScreener) and API query outputs (FRED) that feed downstream Power Query transforms.*

| Sheet | Source | Purpose |
|---|---|---|
| **ForwardEst_Raw** | VBA (`modExtraction` Stage 0.5) | MarketScreener forward estimates staging table (`tblForwardEst_Raw`). Net Sales, EBITDA, EBIT, Net Income for NFY/NFY+1/NFY+2. |
| **ALL_Rates** | Power Query (`FRED`) | FRED interest rates: Federal Funds Rate, Overnight SOFR, Bank Prime Loan Rate, ICE BofA corporate spreads (Master/AAA/AA/A/BBB), US Treasury 20yr. |

### Group 4 — ETL Output — Long/Tall Tables (7 sheets)

*Power Query load destinations. All use `Key` column (`ticker|line item`) as primary lookup index.*

| Sheet | Query | Source | Content |
|---|---|---|---|
| **ALL_IS** | `ALL_IS` | stockanalysis.com | Combined income statements, all tickers |
| **ALL_BS** | `ALL_BS` | stockanalysis.com | Combined balance sheets, all tickers |
| **ALL_CFS** | `ALL_CFS` | stockanalysis.com | Combined cash flow statements, all tickers |
| **ALL_Ratio** | `ALL_Ratio` | stockanalysis.com | Combined ratios (EV, Market Cap, multiples), all tickers |
| **ALL_Beta** | `ALL_Beta` | stockanalysis.com | Combined beta values, all tickers |
| **ALL_ForwardEst** | `ALL_ForwardEst` | `tblForwardEst_Raw` (MarketScreener via VBA) | Combined forward estimates, all tickers |
| **ALL_FINANCIALS** | `ALL_FINANCIALS` | All fetcher functions directly | Master unified lookup table — source of truth for calc layer |

### Group 5 — Reference / Mapping / Helper Tables (4 sheets)

| Sheet | Purpose |
|---|---|
| **Line Item Needs** | Required line items for the model — mapping/checklist |
| **RatioCatalog** | Ratio definitions feeding toggle tables on `Dash_Prjctn` for GPC and subject comparisons. Functionally part of the calculation layer. |
| **Chart Helper** | Data feeder for charts. Structures data for chart consumption. |
| **ScaleFactors** | Unit scale dropdown arrays and scale factor lookups. Candidate for eventual migration to code — currently left in place to avoid breaking the model. |

### Group 6 — Financials (5 sheets)

*Historical and projected financial statements for subject company and GPCs.*

| Sheet | Scope | Status |
|---|---|---|
| **IS** | Subject company income statement | ⚠️ Pending: `if(CompanyStatus="Publicly Traded")` two-way binding |
| **BS** | Subject company balance sheet | ⚠️ Pending: same two-way binding |
| **GPC_IS** | GPC income statements | Active |
| **GPC_BS** | GPC balance sheets | Active |
| **Historic Capital Structure** | Historical debt/equity structure | ⚠️ **UNUSED** — planned as WACC supplement; awaiting data/structure decisions |

### Group 7 — Subject Company Model — Calculation (10 sheets)

*All valuation approaches and supporting schedules.*

| Sheet | Approach / Purpose | Status |
|---|---|---|
| **WACC** | Weighted average cost of capital | Active |
| **DCF** | Discounted cash flow valuation | Active |
| **NWC** | Net working capital schedule | Active |
| **GPC** | Guideline Public Company comps | Active |
| **GT** | Guideline Transaction comps | Active |
| **NAV** | Net Asset Value approach | Active |
| **Summary** | Consolidated valuation summary | ⚠️ **CANDIDATE FOR DELETION** |
| **Tax Depreciation** | Tax depreciation schedule | ⚠️ **UNUSED — CANDIDATE FOR DELETION** if not developed further |
| **Amortization** | Amortization schedule | ⚠️ **UNUSED — CANDIDATE FOR DELETION** if not developed further |
| **NOL** | Net operating loss carryforward | ⚠️ **CANDIDATE FOR DELETION** — NOL currently applied as discrete adjustments inside DCF; carryforward balance not derivable from public company data |

### Group 8 — Client-Provided / PBC (4 sheets)

*Provided by Client data entry area. In the Excel model: manual input or links to 10-K/10-Q filings when `CompanyStatus = "Private"`. In future desktop/web application: user input forms.*

| Sheet | Purpose |
|---|---|
| **PBC->** | Section divider |
| **PBC_IS** | Client-provided income statement |
| **PBC_BS** | Client-provided balance sheet |
| **PBC_CFS** | Client-provided cash flow statement |

### Group 9 — Dashboard / Output (1 sheet)

| Sheet | Purpose |
|---|---|
| **Dash_Prjctn** | Projection controls, WACC inputs, comparable stats dashboard with dynamic chart, GPC/subject toggle tables. Primary user-facing model interface. Also an input surface — shares input characteristics with `Control` sheet. |

### Group 10 — Dividers / Scratch (4 sheets)

*Organizational tabs. No functional logic.*

Diagnostics ->, scratchpaper ->, Would be nice ->, Data Pull ->

### Ungrouped — Deletion Candidate (1 sheet)

| Sheet | Status |
|---|---|
| **Market Data** | ⚠️ **CANDIDATE FOR DELETION** — scratch use of `pmlPRICE()` for GPC current prices; no longer needed |

---

## Section 1.1 — Data Ingestion

Power Query fetcher functions and VBA extraction routines that pull raw financial data per ticker.

### Power Query fetcher functions

Each function takes a ticker string and returns a clean, schema-locked table. stockanalysis functions use `Web.Page()` to render JavaScript-driven tables.

| Function | Source | URL Path | Output |
|---|---|---|---|
| `fnIS` | stockanalysis.com | `/financials/` | Income statement rows |
| `fnBS` | stockanalysis.com | `/financials/balance-sheet/` | Balance sheet rows |
| `fnCFS` | stockanalysis.com | `/financials/cash-flow-statement/` | Cash flow rows |
| `fnRatio` | stockanalysis.com | `/financials/ratios/` | Ratio rows (Market Cap, EV, multiples) |
| `fnBeta` | stockanalysis.com | `/` (overview page) | Single beta row |
| `fnForwardEst` | `tblForwardEst_Raw` (local) | N/A — reads from staging table | Forward estimates per ticker |
| `fnFRED` | fred.stlouisfed.org | REST API per series ID | Single observation per FRED series |

### Utility functions

| Function | Purpose |
|---|---|
| `fnCleanFinancialTable` | Standardizes nulls (`"-"`, `"—"`, `"N/A"`, `""` → null), trims text, normalizes column names |
| `fnSchemaLock` | Enforces column schema contract — adds missing columns as null, removes unexpected columns, reorders consistently |

### VBA extraction (`modExtraction`)

| Stage | Action | Output |
|---|---|---|
| Stage 0 — Slug build | POST to MarketScreener `async/search/quick` per ticker | Slug table (e.g. `ROCKET-LAB-CORPORATION-126208072`) |
| Stage 0.5 — Finance scrape | GET MarketScreener finances page per slug; parse HTML table | `ForwardEst_Raw` staging table |

### Output schema (all fetchers except `fnFRED`)

| Column | Description |
|---|---|
| `Key` | `ticker\|line item` — primary lookup key |
| `Ticker` | Lowercase ticker string |
| `Line Item` | Lowercase metric name |
| `TTM` | Trailing twelve months (IS/BS/CFS) or `Current` (Ratios only) |
| `LFY` through `LFY-3` | Historical fiscal year values (mapped from `FY YYYY` source columns) |
| `NFY` through `NFY+2` | Forward estimate values (`fnForwardEst` only) |

> ⚠️ **Known technical debt:** The column names above represent the *intended* schema after the year-mapping refactor. Currently, year columns are hardcoded as literal year strings (`"2025"`, `"2024"`, etc.) in several M files.

---

## Section 1.2 — Tier 1 Transforms

Combiner queries that loop tickers from `tblIngest`, invoke the corresponding fetcher function per ticker, and stack all results into one table per statement type.

| Query | Fetcher Called | Loads To Sheet |
|---|---|---|
| `ALL_IS` | `fnIS` | `ALL_IS` |
| `ALL_BS` | `fnBS` | `ALL_BS` |
| `ALL_CFS` | `fnCFS` | `ALL_CFS` |
| `ALL_Ratio` | `fnRatio` | `ALL_Ratio` |
| `ALL_Beta` | `fnBeta` | `ALL_Beta` |
| `ALL_ForwardEst` | `fnForwardEst` | `ALL_ForwardEst` |

**Ticker source:** `Companies` query reads from `tblIngest` named table on the `Control` sheet.

---

## Section 1.3 — Tier 2 Transform

`ALL_FINANCIALS` is the master ETL output and primary source of truth for the calculation layer. It calls all fetcher functions **directly** (not via Tier 1 queries — this avoids async race conditions), wraps each result in `Table.Buffer`, combines all results into one long/tall table, and applies a final column select and reorder.

**Output columns:** `Key`, `Ticker`, `Line Item`, `TTM`, `Current`, and year columns (currently hardcoded; will be dynamic after year-mapping refactor)

**Lookup pattern:** All calculation layer formulas use `Key` (`ticker|line item`) as the INDEX/MATCH lookup value against this table.

---

## Section 1.4 — Calculation Layer

The calculation layer is built across multiple worksheets. It uses INDEX/MATCH formulas referencing `ALL_FINANCIALS` via the `Key` column, with all values scaled by the `pmlUnits` named range.

**Valuation approaches implemented:**

| Approach | Sheet(s) | Status |
|---|---|---|
| Discounted Cash Flow (DCF) | `DCF`, `WACC`, `NWC` | Active |
| Guideline Public Company (GPC) | `GPC`, `GPC_IS`, `GPC_BS` | Active |
| Guideline Transaction (GT) | `GT` | Active |
| Net Asset Value (NAV) | `NAV` | Active |

**Key model parameters (from Control sheet named ranges):**

| Parameter | Named Range | Current Value |
|---|---|---|
| WACC | `WACC` | 12.54% |
| Long-term growth rate | `LTGR` | 3.0% |
| DCF historical years | `dcf_HistoricalYears` | 3 |
| DCF projection years | `dcf_ProjectionYears` | 10 |
| IS historical years | `is_HistoricalYears` | 5 |
| IS projection years | `is_ProjectionYears` | 10 |
| NWC projection years | `nwc_ProjectionYears` | 10 |
| Cost count (NAV) | `eCostCount` | 5 |
| Projection years (Dash) | `eProjectionYears` | 10 |
| GPC multiple | `gpcMultiple` | NFY+2 EBITDA |
| GT multiple | `gtMultiple` | TTM Revenue |
| Multiple count | `MultipleCount` | 7 |
| Valuation approach | `Val_Approach` | DCF |

---

## Section 1.5 — Report Output (In Progress)

The report output layer lives on the `Dash_Prjctn` and `Summary` sheets. `Dash_Prjctn` serves as both the primary user-facing dashboard and the projection control surface. It includes:

- Projection toggles (via `modProjectionToggle`)
- WACC input controls
- Comparable multiples display with dynamic chart (via `modCharts`)
- GPC and subject company toggle tables (driven by `RatioCatalog`)
- Reconciliation of values visualization

`Summary` is currently a candidate for deletion — its functionality may be absorbed into `Dash_Prjctn`.

---

## VBA Architecture

### Modules (11)

| Module | Responsibility |
|---|---|
| `modETL_Global` | Global state variables (`PML_IS_RUNNING`, `LOG_SHEET`, constants) |
| `modETL_Refresh` | Main pipeline orchestrator (`Run_ETL_Pipeline`), async query runner |
| `modETL_Logging` | `LogEvent` writer to `ETL_LOG` sheet |
| `modExtraction` | MarketScreener slug build (Stage 0) + finance scrape (Stage 0.5); includes `DumpFailedFinanceHTML` diagnostic |
| `modRibbonCallbacks` | All ribbon button handlers (`RefreshSourceData_Click`, `RefreshBeta_Click`, `RefreshForwardEst_Click`, `ShowETLLog_Click`, `ClearETLLog_Click`, `ExportCode_Click`, etc.) |
| `modPriceFunctions` | Custom worksheet function `pmlPRICE()` — Yahoo Finance live price |
| `modDiagnostics` | Diagnostic / testing utilities |
| `modCharts` | Comparison chart refresh logic |
| `modNAVtoggle` | NAV approach toggle logic |
| `modProjectionToggle` | Projection display toggle logic |
| `modCodeExport` | One-click code exporter — dumps all VBA modules + Power Query M code to local repo |

### ETL pipeline execution order
[Ribbon: Refresh Source Data] → RefreshSourceData_Click
│
├─ Stage 0: Slug Build (VBA, synchronous)
│ └─ POST to MarketScreener per ticker → write slugs
│
├─ Stage 0.5: Finance Extraction (VBA, synchronous)
│ └─ GET MarketScreener per slug → parse → write ForwardEst_Raw
│ └─ On failure: dump HTML to %TEMP%\Canneberge_FailedFinance
│
├─ Stage 1: Power Query Refresh (async, triggered in sequence)
│ └─ ALL_IS → ALL_BS → ALL_CFS → ALL_Ratio → ALL_Beta → ALL_ForwardEst
│
└─ Stage 2: Master Table Refresh (async)
└─ ALL_FINANCIALS


### Performance

| Metric | Value |
|---|---|
| Typical ticker count | 9 GPCs + 1 subject = 10 |
| VBA extraction (Stages 0 + 0.5) | ~30 seconds |
| Power Query refresh (Stages 1 + 2) | ~24 minutes |
| Total end-to-end | ~25 minutes |

### Diagnostic infrastructure

| Feature | Location | Purpose |
|---|---|---|
| `ETL_LOG` sheet | Written by `LogEvent` | Event-level pipeline logging (timestamps, status codes, response sizes) |
| `DumpFailedFinanceHTML` | `modExtraction` | Auto-dumps failed MarketScreener HTML responses to `%TEMP%\Canneberge_FailedFinance\` |
| `Test Connection` button | Ribbon → Diagnostics Tools | Connection health check |

---

## Power Query Inventory (18 queries)

### Output queries (8) — load to sheets

| Query | Type | Loads To |
|---|---|---|
| `ALL_IS` | Tier 1 combiner | `ALL_IS` sheet |
| `ALL_BS` | Tier 1 combiner | `ALL_BS` sheet |
| `ALL_CFS` | Tier 1 combiner | `ALL_CFS` sheet |
| `ALL_Ratio` | Tier 1 combiner | `ALL_Ratio` sheet |
| `ALL_Beta` | Tier 1 combiner | `ALL_Beta` sheet |
| `ALL_ForwardEst` | Tier 1 combiner | `ALL_ForwardEst` sheet |
| `ALL_FINANCIALS` | Tier 2 master | `ALL_FINANCIALS` sheet |
| `FRED` | Rate lookup | `ALL_Rates` sheet |

### Ticker source (1) — connection only

| Query | Purpose |
|---|---|
| `Companies` | Reads `tblIngest` from `Control` sheet; provides ticker list to all combiners |

### Fetcher functions (7) — connection only

| Query | Source | Purpose |
|---|---|---|
| `fnIS` | stockanalysis.com | Income statement per ticker |
| `fnBS` | stockanalysis.com | Balance sheet per ticker |
| `fnCFS` | stockanalysis.com | Cash flow statement per ticker |
| `fnRatio` | stockanalysis.com | Ratios per ticker |
| `fnBeta` | stockanalysis.com | Beta per ticker |
| `fnForwardEst` | `tblForwardEst_Raw` | Forward estimates per ticker (reads VBA staging table) |
| `fnFRED` | fred.stlouisfed.org | Single FRED series observation |

### Utility functions (2) — connection only

| Query | Purpose |
|---|---|
| `fnCleanFinancialTable` | Null standardization, text trimming, column name normalization |
| `fnSchemaLock` | Column schema enforcement — add missing, remove unexpected, reorder |

---

## Named Range Inventory

### Year Anchors

| Named Range | Current Value | Semantic | Refers To |
|---|---|---|---|
| `FiscalYearEnd` | 12/31/2025 | Last Fiscal Year (LFY) | `=Control!$F$21` |
| `FiscalQuarter` | 3/31/2026 | Most recent fiscal quarter | `=Control!$Q$11` |
| `NextFiscalYear` | 12/31/2026 | Next Fiscal Year (NFY) | `=Control!$F$20` |
| `NFY_1` | 12/31/2027 | NFY + 1 | `=Control!$F$21` |
| `NFY_2` | 12/31/2028 | NFY + 2 | `=Control!$F$22` |
| `pmlYears` | `{"TTM","2025",...,"2028"}` | Year label array | `=Control!$AJ$26:$AJ$35` |

### Subject Company

| Named Range | Current Value | Refers To |
|---|---|---|
| `SubjectCompanyTicker` | SPCX | `=Control!$F$10` |
| `SubjectName` | SpaceX | `=Control!$F$16` |
| `CompanyStatus` | Publicly Traded | `=Control!$F$14` |
| `ClientName` | Ted & Co. | `=Control!$F$9` |
| `SubjectTaxRate` | 21% | `=Control!$F$16` |
| `MainTitle` | Sensitivity Analysis of SpaceX | `=Control!$F$11` |
| `StandardOfValue` | Fair Market Value | `=Control!$Q$16` |

### Model Parameters

| Named Range | Current Value | Refers To |
|---|---|---|
| `dcf_HistoricalYears` | 3 | `=DCF!$E$3` |
| `dcf_ProjectionYears` | 10 | `=DCF!$F$3` |
| `is_HistoricalYears` | 5 | `=IS!$D$2` |
| `is_ProjectionYears` | 10 | `=IS!$D$3` |
| `nwc_ProjectionYears` | 10 | `=NWC!$E$2` |
| `eCostCount` | 5 | `=Dash_Prjctn!$H$50` |
| `eProjectionYears` | 10 | `=Dash_Prjctn!$B$29` |
| `gpcMultiple` | NFY+2 EBITDA | `=Dash_Prjctn!$BG$29` |
| `gtMultiple` | TTM Revenue | `=Dash_Prjctn!$BA$29` |
| `Multiple1` | NFY+2 EBITDA | `=GPC!$E$2` |
| `MultipleCount` | 7 | `=Dash_Prjctn!$BA$29` |
| `Val_Approach` | DCF | `=DCF!$E$12` |
| `WACC` | 12.54% | `=WACC!$J$76` |
| `LTGR` | 3.0% | `=Control!$F$49` |
| `Taxable` | Taxable | `=Control!$Q$17` |

### Unit Scaling

| Named Range | Current Value | Refers To |
|---|---|---|
| `pmlUnits` | 1.00 | `=ScaleFactors!$C$8` |
| `pmlFwdUnits` | 1.00 | `=ScaleFactors!$C$13` |
| `NumericScale` | Millions | `=Control!$Q$11` |
| `pmlScale` | (dropdown array) | `=ScaleFactors!$B$18:$B$22` |
| `pmlUnitsArray` | (dropdown array) | `=ScaleFactors!$B$18:$D$22` |

### Data Tables

| Named Range | Refers To |
|---|---|
| `ALL_FINANCIALS` | `=ALL_FINANCIALS!$A$2:$M$1394` |
| `ALL_ForwardEst` | `=ALL_ForwardEst!$A$2:$F$41` |
| `tblForwardEst_Raw` | `=ForwardEst_Raw!$A$2:$F$41` |
| `tblFREDSeries` | `=Control!$B$28:$B$42` |
| `tblIngest` | `=Table7[#All]` |
| `FRED` | `=ALL_Rates!$A$2:$D$10` |

### Miscellaneous

| Named Range | Current Value | Refers To |
|---|---|---|
| `KeyFRED` | (API key) | `=Control!$F$49` |
| `vDate` | 7/3/2026 | `=Control!$Q$10` |
| `vDateProper` | As of July 3, 2026 | `=Control!$D$75` |
| `Ratio1` | 3-Year Hist. Revenue CAGR | `=Dash_Prjctn!$BA$6` |
| `Print_Area` | (range) | `=Dash_Prjctn` |

---

## Known Limitations

- `Web.Page()` renders a full browser engine per fetch — 6 functions × N tickers = significant runtime (~24 min for 10 tickers)
- Yahoo Finance `v10/quoteSummary` endpoint now requires crumb authentication — not usable unauthenticated; `pmlPRICE` uses `v8/finance/chart/` instead
- `"Pro"` paywalled values on stockanalysis.com are automatically nulled out
- VBA async pipeline means `ALL_FINANCIALS` cannot safely reference Tier 1 query outputs (race condition risk) — it calls fetcher functions directly instead
- MarketScreener has a daily pull limit — exceeding it returns a ~284KB generic page; auto-dumped to `%TEMP%\Canneberge_FailedFinance\` for diagnosis
- `ETL_LOG` auto-clears on each pipeline run — no historical log retention

---

## Known Technical Debt

| Item | Files Affected | Status |
|---|---|---|
| Hardcoded year references | `modExtraction`, `fnForwardEst.m`, `fnSchemaLock.m`, `ALL_FINANCIALS.m`, `fnIS.m`, `fnBS.m`, `fnCFS.m`, `fnRatio.m` | Queued for year-mapping refactor |
| `if(CompanyStatus)` two-way binding | `IS`, `BS` sheets | Pending implementation |
| `pmlYears` named range | Hardcoded array; should compute from `FiscalYearEnd` | Part of year-mapping refactor |
| `ETL_STATE` (Sheet5) | VBA codename exists but no visible tab | Investigate: hidden or orphaned? |
| Deletion candidates | `Summary`, `Tax Depreciation`, `Amortization`, `NOL`, `Market Data` | Review and decide — **do not delete yet** |
| `ScaleFactors` sheet | Could be handled in code | Low priority — leaving in place to avoid breaking model |